#include <analyse.h>


int *filterStudentNumbers(int *student_numbers, SNumsInfo students_info, 
size_t size);

// int *extractStudentNumbers(char *text, SNumsInfo students_info);

// int *readStudentNumbers(char *file, SNumsInfo students_info);
